const express = require('express');
const router = express.Router();

// This will be populated with route handlers
// For now, we'll keep everything in server.js for simplicity

module.exports = router;